const NGE = (array) => {
    let lenMinus2 = length(array)-2
    for(let i=1; i<lenMinus2; i++){
        let n = i +1
        while (i < lenMinus2)
            if(n -i > 0) {
                Push
                continue
            }
            n= n+1
    }
}